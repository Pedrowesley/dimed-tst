import { FilterLineBusPipe } from './filter-line-bus.pipe';

describe('MyfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLineBusPipe();
    expect(pipe).toBeTruthy();
  });
});
